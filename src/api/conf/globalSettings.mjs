// SECURITY WARNING: keep the secret key used in production secret!
const SECRET_KEY = "";

// SECURITY WARNING: don't run with debug turned on in production!
const DEBUG = false;

// Database settings
const DATABASES = {};

// Internationalization
const TIME_ZONE = "UTC";
const USE_TZ = true;

export default { SECRET_KEY, DEBUG, DATABASES, TIME_ZONE, USE_TZ };
