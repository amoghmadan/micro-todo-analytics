DEBUG = "true"

# Base URL of the existing api-gateway (GraphQL + REST accounts).
API_GATEWAY_URL = "http://localhost:4000"

# Secret used to sign the httpOnly session cookie.
SECRET_KEY = "%*d9(s7cczfp+o%v894y&1w-g*318&72j_7v47#8-$a*$jc5+v"

COOKIE_NAME = "web_gateway_session"

# Optional. Override whether the session cookie requires HTTPS. When unset, the
# gateway infers it from the request (X-Forwarded-Proto / https), so plain-HTTP
# local development works without DEBUG = true.
# COOKIE_SECURE = true
